To install the dependencies, open your command line, navigate to the directory where this folder is and type: 

                                          npm install

You need to have npm and Node to install the dependencies/run the app
